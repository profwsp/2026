# 📊 Arquitetura Visual - IndusMonitor 4.0

## Estrutura de Navegação

```
┌─────────────────────────────────────────────────────────┐
│                    App.js                               │
│                (Entrada Principal)                      │
└────────────────────┬────────────────────────────────────┘
                     │
           ┌─────────▼──────────┐
           │ PaperProvider      │
           │ (Tema Dark Ind.)   │
           └────────┬───────────┘
                    │
         ┌──────────▼───────────┐
         │NavigationContainer   │
         │ (React Navigation)   │
         └────────┬─────────────┘
                  │
     ┌────────────┼────────────┐
     │            │            │
┌────▼───┐  ┌────▼────┐  ┌───▼─────┐
│TAB 1   │  │  TAB 2  │  │  TAB 3  │
│Dashboard│  │ Scanner │  │ Sensores│
└────┬───┘  └────┬────┘  └───┬─────┘
```

## Hierarquia de Components

### Dashboard Screen
```
DashboardScreen
├── Appbar.Header
│   └── Appbar.Content
├── ScrollView
│   ├── Status Summary Container
│   │   ├── StatusChip (Operando)
│   │   ├── StatusChip (Alerta)
│   │   └── StatusChip (Parada)
│   ├── Máquinas Section
│   │   └── MachineCard (x múltiplas)
│   │       ├── Header
│   │       │   ├── TitleContainer
│   │       │   └── StatusChip
│   │       └── MetricsContainer
│   │           ├── MetricItem (Temperatura)
│   │           ├── Divider
│   │           └── MetricItem (Vibração)
└── FAB (Atualizar)
```

### Scanner Screen
```
ScannerScreen
├── Appbar.Header
│   └── Appbar.Content
├── ScannerContainer
│   ├── BarCodeScanner (Camera)
│   ├── Overlay
│   │   ├── FocusedContainer
│   │   └── Corner Indicators
│   └── CameraHint (ou ResultContainer)
│       └── ScannedData Display
└── Snackbar (Feedback)
```

### Sensors Screen
```
SensorsScreen
├── Appbar.Header
│   └── Appbar.Content
├── ScrollView
│   ├── StatusCard
│   │   ├── StatusLeft
│   │   └── StatusRight
│   ├── SegmentedButtons (Eixos)
│   ├── ChartCard
│   │   └── LineChart (SVG)
│   ├── MetricsCard
│   │   ├── ValueBox (X)
│   │   ├── Divider
│   │   ├── ValueBox (Y)
│   │   ├── Divider
│   │   └── ValueBox (Z)
│   └── InfoBox
└── [ScrollView End]
```

## Fluxo de Dados

```
┌─────────────────────────────────────────────┐
│   DashboardScreen Component State           │
│   machines: Array<Machine>                  │
│   - id, name, status, temp, vibration       │
└──────────────┬────────────────┬─────────────┘
               │                │
        ┌──────▼────┐    ┌─────▼──────┐
        │getStatus  │    │MachineCard │
        │Count()    │    │(props)     │
        └───────────┘    └────────────┘
               │                │
      ┌────────▼──────────┬─────▼─────┐
      │StatusChips        │Metrics    │
      │Display            │Display    │
      └───────────────────┴───────────┘
```

## Fluxo do Scanner

```
┌─────────────────────────────────────┐
│  ScannerScreen Component            │
│  - hasPermission: boolean           │
│  - scanned: boolean                 │
│  - scannedData: { type, data }      │
└──────────┬──────────────────────────┘
           │
    ┌──────▼──────┐
    │ Permission? │
    └──────┬──────┘
      Yes  │  No
     ┌─────┴─────┐
  ┌──▼──┐   ┌────▼────┐
  │Scan │   │Error    │
  │Mode │   │Message  │
  └──┬──┘   └─────────┘
     │
  ┌──▼──────────────────┐
  │ Scanned = false?    │
  └──┬──────────┬───────┘
   Yes          No
    │           │
┌───▼──┐   ┌────▼──────┐
│Listen│   │Show Result│
│Code  │   │Container  │
└──┬───┘   └───────────┘
   │
   ├─ Code Detected
   │
┌──▼────────────────────┐
│handleBarCodeScanned() │
│- setScanned(true)     │
│- setScannedData()     │
│- Snackbar.show()      │
└───────────────────────┘
```

## Fluxo do Monitoramento de Sensores

```
┌──────────────────────────────────────┐
│  SensorsScreen Component             │
│  - data: Array<SensorData>           │
│  - selectedAxis: string              │
│  - isMonitoring: boolean             │
└──────┬───────────────────────────────┘
       │
       ├─ useEffect
       │  └─ Accelerometer.addListener()
       │     └─ Update interval: 500ms
       │
       └─ setData()
          └─ Slice last 100 points
             │
   ┌─────────┼─────────┐
   │         │         │
┌──▼──┐ ┌───▼───┐ ┌──▼──┐
│getX │ │getY   │ │getZ │
│Axis │ │Axis   │ │Axis │
└─────┘ └───────┘ └─────┘
   │         │         │
   └─────────┼─────────┘
             │
      ┌──────▼──────┐
      │getChart     │
      │Data()       │
      │- Combined   │
      │- Axis data  │
      └──────┬──────┘
             │
      ┌──────▼──────────┐
      │LineChart Render │
      └─────────────────┘
```

## Estados Visuais

### MachineCard Estados

```
┌─────────────────────────────────────┐
│ OPERANDO                            │
│ ├─ Borda Esquerda: Verde (#00E676) │
│ ├─ Chip: Verde "● Operando"        │
│ └─ Cores: Cyan para valores        │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ ALERTA                              │
│ ├─ Borda Esquerda: Orange (#FF6B00)│
│ ├─ Chip: Orange "⚠ Alerta"        │
│ └─ Cores: Orange para valores      │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ PARADA                              │
│ ├─ Borda Esquerda: Red (#FF5252)   │
│ ├─ Chip: Red "■ Parada"            │
│ └─ Cores: Red para valores         │
└─────────────────────────────────────┘
```

### Vibração Estados

```
Normal (🟢)     < 1.0 m/s²    
Alerta (🟠)     1.0 - 3.0 m/s²
Crítico (🔴)    > 3.0 m/s²
```

## Layout de Responsividade

```
Phone Portrait (320-480px)
┌────────────────────┐
│                    │  Header
├────────────────────┤
│                    │
│   Status Summary   │  Scroll
│                    │  Area
├────────────────────┤
│  Machine Card 1    │
├────────────────────┤
│  Machine Card 2    │
│       ...          │
├────────────────────┤
│  Dashboard │Scan│S │  Tabs

Phone Landscape (800-1024px)
┌──────────────────────────────────┐
│          Header + Icon           │
├──────────┬──────────┬────────────┤
│Status 1  │ Status 2 │ Status 3   │
├──────────┴──────────┴────────────┤
│  Machine Card 1   │ Machine Card 2│
├───────────────────┬───────────────┤
│  Machine Card 3   │ Machine Card 4│
└───────────────────┴───────────────┘
```

## Mapa de Cores por Contexto

```
Status Cards
  Operando  → Verde     (#00E676)
  Alerta    → Orange    (#FF6B00)
  Parada    → Vermelho  (#FF5252)

UI Primário
  Fundos    → Dark      (#0A0E27, #1F2847)
  Bordas    → Dark      (#2D3561)
  Textos    → Cinza     (#E0E0E0, #A0A0A0)

Destaques
  Primário  → Cyan      (#00BCD4)
  Secundário→ Orange    (#FF6B00)
  Success   → Verde     (#00E676)
```

## Performance & Otimização

```
useCallback() para functions em:
├─ handleBarCodeScanned()
├─ getStatusCount()
└─ getVibrationStatus()

useMemo() para derivações de state:
├─ chartData()
├─ vibrationStatus()
└─ averageVibration()

Lazy Loading:
├─ Screens carregadas ao serem selecionadas
└─ Assets otimizados (PNG, WebP)

Cache:
├─ Últimos 100 pontos de vibração
└─ Dados de máquinas em estado local
```

---

**Documentação da Arquitetura Visual - IndusMonitor 4.0**
